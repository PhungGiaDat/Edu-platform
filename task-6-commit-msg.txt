Task 6: replace inferred migration with exact idempotent repair

Add backend/database/migrations/repair_ar_objects_consistency.py that
builds a deterministic, opt-in repair plan from the checked-in
ar_object_consistency_map.json. The CLI supports --dry-run (default) and
--apply with --expected-db; no writes happen without explicit apply.

Add backend/tests/test_repair_ar_objects_consistency.py covering catalog +
legacy paths, compare-and-set filter shape, and idempotency on a clean
dataset. 8/8 Phase B pytest cases pass.
