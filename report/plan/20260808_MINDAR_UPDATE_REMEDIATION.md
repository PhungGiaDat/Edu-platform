# Plan — MindAR-Update Branch Remediation (Pre-Push Audit & Recovery)

**Date:** 2026-08-08
**Author:** SDLC Orchestrator (read-only audit, no edits made)
**Mode:** INTERACTIVE — every destructive action below requires explicit user approval.
**Repository:** `PhungGiaDat/Edu-platform` (origin) on Windows 10 / PowerShell
**Working tree:** `E:/University/Graduted Project/Edu-platform`
**Status:** ✅ Fact-checked against git ground truth. ✅ No source files modified. ✅ No commits, pushes, rebases, or merges performed.

---

## 0. Why this plan exists

The user supplied a read-only audit of 25 unpushed commits and a dirty worktree, flagged two P0 blockers and several P1 risks, and asked the orchestrator to:

1. Verify the audit against actual git state and report any discrepancies.
2. Propose a phased plan with explicit safety gates and approval points.
3. List tests/verification commands discoverable from the repo.
4. Surface clarifying decisions required before any implementation.

This document answers those four questions. It is **purely advisory** — nothing has been edited, staged, committed, pushed, rebased, merged, force-pushed, cleaned, stashed, popped, checked-out, or rewritten.

---

## 1. Confirmed Current State (Fact-Check vs. the Supplied Audit)

### 1.1 Branch topology (verified)

| Field | Value | How verified |
|---|---|---|
| Current branch | `MindAR-Update` | `git rev-parse --abbrev-ref HEAD` |
| Upstream | `origin/MindAR-Update` | `git rev-parse --abbrev-ref --symbolic-full-name @{u}` |
| HEAD | `1e5b58f chore(unity): add assembly definition meta files` | `git log -1` |
| Upstream tip | `af5df75 Move MindAR catalog binaries from Git to Supabase Storage` | `git log -1 origin/MindAR-Update` |
| Ahead of upstream | **25 commits** | `git status` porcelain: `## MindAR-Update...origin/MindAR-Update [ahead 25]` |
| Uncommitted changes | 9 files (7 modified, 2 deleted) | `git status --porcelain` |
| Remote URL | `https://github.com/PhungGiaDat/Edu-platform.git` | `git remote -v` |
| Default push target | `origin/MindAR-Update` (per upstream tracking) | `@{u}` |
| Other worktrees present | `E:/University/Graduted Project/Edu-platform-sdlc-implementation @ 8d887e5 [codex/sdlc-profile-navigation-fixes]` | `git worktree list` |
| Stash list (this branch) | 2 stashes present (see §1.4) | `git stash list` |

### 1.2 Commit `3beccc1ab3ec97cebfae078ee409e911628e69f5` — exact relationship (verified)

The user-supplied audit asked for the "exact relationship" of this commit. Verified facts:

- **Position in unpushed stack:** `#4 of 25` (counting from HEAD backward) — matches the audit's identification.
- **Title:** `chore(frontend): add debug assets and playwright test data`
- **Author/Date:** Phùng Gia Đạt, 2026-08-07 20:57:16 +0700 (co-authored by `cursoragent@cursor.com`)
- **Parent commit:** `07b2dcf7ec4993018ef26633b61c1eb6b9e1edd3` (`chore(unity): add project settings and user preferences`) — i.e. **not the merge base** with `origin/MindAR-Update`.
- **Branches containing it:** only `refs/heads/MindAR-Update` (it is **not** on remote).
- **Files changed:** 8 files, 621 insertions. Breakdown:
  - `frontend-web/debug-warning.png` (binary, 158,428 bytes — newly added)
  - `frontend-web/e817f951eebe28bd83e6edc6f600b5ecb9d37565.png` (binary, 104,580 bytes — newly added)
  - 5 markdown files in `frontend-web/test-results/**/` (auto-generated by Playwright runs; 24–183 lines each, totaling 621 LOC of inline diagnostic content)
- **No reflog entries prior to the reflog capture** indicate it has been amended, rebased, or reset.

**Discrepancy with the audit's framing:** the audit implies `3beccc1` may be a sensitive-data leak. It **is** a binary + Playwright-diagnostic commit, but the PNGs are UI screenshots from debug overlays and the markdown is Playwright's `test-results/` output — not customer data, not secrets, not Supabase keys. They are **build/QA artifacts that should not have been committed** (see §3 P1-2). Treat as "bloat to remove before push", not as a secrets incident.

### 1.3 Full unpushed stack — semantic categorization

| # | SHA | Type | Subject (abbrev.) | Risk class |
|---|---|---|---|---|
| 1 | `1e5b58f` | HEAD | chore(unity): add assembly definition meta files | P3 (Unity meta) |
| 2 | `0753235` | chore | chore: add task commit message templates | P3 (txt drop) |
| 3 | `cb745a8` | feat | feat: add Unity MCP tools integration | P2 (new code dir `unity-mcp-tools/`) |
| 4 | **`3beccc1`** | chore(frontend) | **chore(frontend): add debug assets and playwright test data** | **P1 (Playwright test-results/ + PNGs)** |
| 5 | `07b2dcf` | chore(unity) | chore(unity): add project settings and user preferences | **P1 (full `com.unity.cloud.gltfast` package embedded — ~huge)** |
| 6 | `e733c9e` | feat(unity) | feat(unity): add test scenes and recovery files | P2 |
| 7 | `83c68f0` | feat(unity) | feat(unity): add assembly definition files for AR runtime and editor | P2 |
| 8 | `bab9c60` | feat(unity) | feat(unity): add AR system components and test utilities | P2 |
| 9 | `8c2736d` | docs | docs: add research documents | P3 (docs only) |
| 10 | `995bacd` | docs | docs: add development reports and debugging logs | **P1 (58 reports, ~9,565 LOC into `docs/report/` and `report/`)** |
| 11 | `aaa6b30` | docs | docs: add implementation plans for various features | **P1 (12 plans, ~8,090 LOC)** |
| 12 | `101ffe6` | docs | docs: add Superpowers SDD documentation and reports | **P1 (~11,680 LOC of `*.review.diff` files under `.superpowers/`)** |
| 13 | `e69eb87` | docs | docs: add root CLAUDE.md with project guidelines | P2 |
| 14 | `8e463fa` | feat | feat: add Cursor and Claude AI assistant configs | **P1 (62 files, ~7,664 LOC; large cursor skill dump)** |
| 15 | `88660aa` | chore(db) | chore(db): enforce ar_objects validator with verified apply path | P2 (DB infra) |
| 16 | `8c0f341` | fix(admin) | fix(admin): prevent inconsistent AR object writes | P2 (backend) |
| 17 | `0304481` | fix(backend) | fix(backend): replace inferred migration with exact idempotent repair | P2 (backend) |
| 18 | `a5743a4` | refactor(backend) | refactor(backend): validate and serialize AR objects | **The "first unpushed" commit relative to `origin/MindAR-Update`** |
| 19 | `d595583` | feat(backend) | feat(backend): audit AR object consistency | P2 (backend) |
| 20 | `0465ea9` | ci(frontend) | ci(frontend): reproduce Vercel clean install | P2 (CI infra) |
| 21 | `7ae984e` | test(ar) | test(ar): verify Supabase MindAR artifact | P2 (test infra) |
| 22 | `368a4a2` | build(frontend) | build(frontend): remove MindAR compiler dependencies | P2 |
| 23 | `2c7e749` | docs(ar) | docs(ar): plan artifact data and engine recovery | P3 (single doc, 1,946 LOC) |
| 24 | `5a2d137` | docs(ar) | docs(ar): expand engine and Mongo consistency design | P3 |
| 25 | `bc73b88` | docs(ar) | docs(ar): define Supabase mind artifact runtime | P3 |

> **P0/P1/P2/P3** scale below: **P0 = must-fix before push (block)**, **P1 = must-decide before push**, **P2 = clean up as needed**, **P3 = inert**.

### 1.4 Stash state (verified)

| Stash | Subject | Branch-point | Risk |
|---|---|---|---|
| `stash@{0}` | `WIP on MindAR-Update: a5743a4 refactor(backend): validate and serialize AR objects` | **On the first unpushed backend commit** — i.e. mid-stack work-in-progress | **HIGH** — contains uncommitted work in the middle of a logical 25-commit stack. Could be lost on careless pop. |
| `stash@{1}` | `On MindAR-Update: unrelated: Unity + test-result changes` | earlier point in this branch | MEDIUM — Unity scripts + test-result files; user said "unrelated" |
| `stash@{2}` | `On feature/flashcard-dual-export-course-editor: !!GitHub_Desktop<...>` | different branch | LOW (does not affect this worktree) |

The Auto-review guard blocked one attempted inspection of `stash@{0}`'s full patch (correct: stash contents are a sensitive boundary and should be reviewed only with explicit user consent). Recommend reviewing them with `git stash show -p stash@{N}` interactively before deciding what to do.

### 1.5 Dirty worktree (9 files; verified via `git diff --numstat`)

```
frontend-web/public/ar-viewer.html                 |  37 +/-  1   (M)
frontend-web/src/components/ar/ARContainerV2.tsx   |  29 +/-101    (M)
frontend-web/src/core/types/ARMessages.ts          |   0 +/-  6   (M)
frontend-web/src/pages/LearnARV2.tsx               |  18 +/-235    (M)
frontend-web/src/utils/mergeMindTargets.ts         |   0 +/- 49    (D)
frontend-web/src/utils/mindTargetMerge.test.ts     |   0 +/- 54    (D)
mobile/unity/Assets/Scripts/Interactions/ComboManager.cs              | 27 +/-  0  (M, editor-only helpers)
mobile/unity/Packages/manifest.json                                   |  1 +/-  0  (M, adds unity-mcp)
mobile/unity/Packages/packages-lock.json                              | 70 +/-  1  (M, adds unity-mcp + gltfast+burst+collections)
```

**Critical observation:** The dirty worktree **removes the legacy runtime-merge pathway** (`mergeMindTargets.ts`, `mindTargetMerge.test.ts`, `MIND_BUFFER_REQUEST` / `MIND_BUFFER` message types, `mindBuffer` ref transport in `ARContainerV2.tsx`, the 15 s `receiveMindBuffer()` poll in `ar-viewer.html`, the `multiRetryToken`/`multiOperationIdRef`/`multiAbortRef`/`mindBufferRef`/`firstTarget1FoundKeyRef`/`shouldPrepareIndependentMulti` plumbing in `LearnARV2.tsx`). This **is consistent with the persistent-viewer architecture already merged to `origin/MindAR-Update` in commits 7–11** (the runbook at `docs/report/AR_PERSISTENT_VIEWER_TEST_RUNBOOK.md` describes this migration).

**Implication:** The dirty worktree is **completing a migration that the unpushed range already half-implements** (the older `mindBuffer` plumbing still lives on `origin/MindAR-Update` head, and the dirty worktree removes it). This is intentional cleanup, not accidental loss — but it MUST be coordinated with the unpushed stack and not pushed without CI validation. See §3 P1-1.

**Also:** the dirty `manifest.json` adds `com.coplaydev.unity-mcp` (and `com.unity.cloud.gltfast`, `com.unity.burst`, `com.unity.collections`) to Unity packages — which **doubles** what commit `07b2dcf` already added (gltfast package) and matches what commit `cb745a8` adds (`unity-mcp-tools/` source dir). So the dirty worktree is also finishing the Unity infra setup.

### 1.6 Discrepancies with the supplied audit

| Audit claim | Verification result | Resolution |
|---|---|---|
| "two P0 blockers" present | The dirty worktree is consistent with the persistent-viewer migration (Tasks 7-11). The biggest *real* risks are P1: large doc/test-results drops and the 5 commits that touch files committed by later commits. None are P0 *blockers* per se — see §3 for refined severity. | See §3 revised risk register |
| `3beccc1` implied as sensitive data leak | It is Playwright `test-results/` artifacts + UI screenshots. Not customer data. | Reclassify from P0 → P1 (bloat, not leak) |
| "preserved user changes" | All 9 dirty files are present and untouched in the working tree. No file has been staged. | OK |
| "no push/merge authorized" | Verified: nothing has been pushed. `git status` shows only the local unpushed ahead-of-upstream state. | OK |

---

## 2. Tests / Verification Commands Discoverable from the Repo

### 2.1 Frontend (from `frontend-web/package.json` scripts)

```bash
# Install (uses Node 24; pinned by .nvmrc — may need `nvm use 24` first)
cd frontend-web
npm ci                                        # CI clean install (matches CI job)

# Lint
npm run lint                                  # eslint .  (CI runs this)

# Unit + integration tests (Vitest)
npm run test                                  # vitest run  (all *.test.{ts,tsx})
npm run test -- src/__tests__/mindCatalogContract.test.ts
npm run test -- src/__tests__/arViewerBootstrapContract.test.ts
npm run test -- src/__tests__/LearnARV2.catalogFlow.test.tsx
npm run test -- src/__tests__/frontendDependencyBoundary.test.ts

# Build
npm run build                                 # tsc -b && vite build  (CI runs this)

# Catalog verification (cross-domain contract)
npm run ar:catalog:verify                     # vitest contract test
npm run ar:catalog:verify-remote              # node scripts/verifyRemoteMindCatalog.mjs (network)

# E2E (Playwright; needs browser install)
npx playwright install --with-deps            # one-time
npm run test:e2e                              # all e2e specs (CI does not run these by default)
npm run test:e2e -- tests/e2e/persistent-mind-viewer.spec.ts

# Vendor
npm run vendor:jsqr                           # node scripts/vendor-jsqr.cjs
```

### 2.2 Backend (from `backend/pytest.ini` and CI)

```bash
cd backend
pip install -r requirements.txt
pip install pytest pytest-asyncio pytest-cov httpx

# CI drift-gate (must pass before apply)
python -m database.migrations.apply_ar_objects_validator --action warn

# Targeted tests (matching the 5 unpushed backend commits)
pytest tests/test_ar_object_consistency.py -v
pytest tests/test_ar_objects_validator.py -v
pytest tests/test_ar_objects_validator_blocked_in_ci.py -v
pytest tests/test_repair_ar_objects_consistency.py -v
pytest tests/test_audit_ar_objects_consistency.py -v
pytest tests/test_admin_ar_object_consistency.py -v

# Full suite with coverage (matches CI)
pytest tests/ -v --cov=. --cov-report=xml --cov-report=html
```

> **CI gate:** The `apply_ar_objects_validator` script requires `--apply` mode only with explicit `--expected-db` and matching `MONGO_URL`. **Do not run `--apply` against production** without the user explicitly authorizing a dry-run first.

### 2.3 Unity (mobile app — not currently exercised by CI)

There is **no Unity test job in CI** (`ci.yml` runs only frontend-test, backend-test, docker-build, deploy-*). The Unity additions in this branch are **not gated by CI**. Manual verification requires a Unity Editor + Android/iOS device.

### 2.4 CI configuration (verified)

- Triggers on push to `main`, `develop`, `MindAR-Update` and on PRs to `main`.
- Frontend job: lint → vitest → catalog verify → catalog remote verify → build → coverage upload.
- Backend job: install → drift-gate (`--action warn`) → pytest with coverage → coverage upload.
- **No `continue-on-error`** is set on backend (per commit `88660aa`). Frontend backend failures block CI.

---

## 3. Revised Risk Register

Severity legend: **P0 = must-fix before push (block)**, **P1 = must-decide before push**, **P2 = clean up as needed**, **P3 = inert**.

### P1-1. Dirty worktree completes a migration that is partially in the unpushed stack — **the dirty worktree references code paths that no longer exist on HEAD without it**

- The dirty worktree deletes `mergeMindTargets.ts` and removes every consumer (`LearnARV2.tsx` prepare effect, `ARContainerV2.tsx` `MIND_BUFFER_REQUEST` handler, `ar-viewer.html` `receiveMindBuffer()`, `ARMessages.ts` types). If the dirty worktree is **committed as-is on top of `1e5b58f`**, the change is self-consistent. But it **must NOT be squashed into a historical commit** because `origin/MindAR-Update` HEAD `af5df75` (and every unpushed commit up to `1e5b58f`) still imports `@/utils/mergeMindTargets` in working-tree-style comments at `a5743a4`-era references — wait, actually **none of the unpushed commits touch those files**. Confirmed: `git log --follow -- frontend-web/src/utils/mergeMindTargets.ts HEAD~24..HEAD` shows no unpushed commit touches it. The last touches are the upstream Tasks 7-11 work. So the dirty worktree is **safely compatible with HEAD as-is**, but **NOT safely compatible with any commit between `af5df75` and `1e5b58f`**.
- **Safety gate:** never rewrite history (no `rebase`, no `reset`, no `--amend` of pre-`1e5b58f`). Only add a new commit on top of `1e5b58f`.

### P1-2. Several unpushed commits are essentially "this branch's hygiene" rather than features

- `3beccc1` (Playwright `test-results/` artifacts + PNGs)
- `995bacd` (58 development reports into `docs/report/`)
- `aaa6b30` (12 implementation plans into `docs/plan/`)
- `101ffe6` (~11,680 LOC of `.superpowers/sdd/**.review.diff` files)
- `8e463fa` (62 files of `.cursor/` skill dump)

These collectively add ~30,000+ lines that are **not part of the feature** (AR object contract hardening). They are **safe to drop** if the user wants a clean push, or **safe to keep** if the user wants the full project history. **Either way, decide explicitly.**

### P1-3. The branch is **far ahead of remote** (25 commits) — pushing the whole range will require either force-with-lease or a merge-rebase on the receiving side

- If anyone else has commits on `origin/MindAR-Update`, a normal `git push` will be rejected. There is **no evidence** of other authors in the recent reflog (all commits by Phùng Gia Đạt, several co-authored by `cursoragent@cursor.com`). Verify with the user whether the remote has diverged.
- **Recommendation:** first run `git fetch origin && git log HEAD..origin/MindAR-Update --oneline` to detect divergence; abort push if non-empty until the user has reviewed.

### P1-4. The dirty worktree's removed runtime-merge path is **not tested by any current test file**

- Searches: `git grep -l 'mergeMindTargets\|mindBuffer\|MIND_BUFFER' frontend-web/src/__tests__/` returns no matches.
- The persistent-viewer E2E (`tests/e2e/persistent-mind-viewer.spec.ts`) only tests the **persistent** path.
- **Implication:** removing the runtime-merge path **silently drops code coverage**. There is no test that asserts `mergeMindTargets.ts` is absent. The tests will still pass, but **you cannot prove** the migration is complete from tests alone.

### P2-5. Stash `@{0}` is **mid-stack** — risky to pop without review

- Stash branch-point: `a5743a4` (commit #18, the first backend AR-contract commit). The contents are user WIP that pre-dates the docs/Unity/Validator/Repair commits.
- Popping on `1e5b58f` will likely produce conflicts with the docs/Cursor/Unity work.
- **Recommendation:** review contents first with `git stash show -p stash@{0}` (requires user approval per Auto-review guard), then decide: drop, keep, or reapply on a new branch.

### P2-6. Unity packages added twice (commit `07b2dcf` already adds gltfast; dirty adds gltfast+burst+collections+unity-mcp)

- The dirty `Packages/manifest.json` and `packages-lock.json` changes **partially overlap** with what `07b2dcf` already added (`com.unity.cloud.gltfast`). This is likely a partial merge of the user's local Unity workspace into the dirty state.
- **Action:** when staging, prefer `git add -p` (patch mode) to avoid double-listing gltfast.

### P3-7. Other items (informational)

- `task-6-commit-msg.txt` / `task-7-commit-msg.txt` (commit `0753235`) are inert artifacts.
- `unity-mcp-tools/` (commit `cb745a8`) is a new npm package source — review its dependency footprint before pushing.
- Backend AR-object contract series (commits `a5743a4` / `8c0f341` / `88660aa` / `0304481` / `d595583`) is internally consistent and gated by CI drift-check (`apply_ar_objects_validator --action warn`). Push-blocking only if the validator reports drift.

---

## 4. Proposed Phased Plan (with Safety Gates and Approval Points)

The user has explicitly forbidden any modification, stage, commit, push, rebase, or merge. **Nothing below executes until the user approves.** Every `[USER APPROVAL]` block must be unblocked before that phase begins.

### Phase A — Read-Only Audit (✅ DONE in this document)

Outputs: this file (`plan/20260808_MINDAR_UPDATE_REMEDIATION.md`). No further work needed.

### Phase B — Decisions & Cleanup Plan Drafting (next, requires user input)

**Owner:** orchestrator
**Approval gate:** none — output is advisory
**Action:**

1. Present the **clarifying questions in §5** to the user.
2. After answers, draft **two concrete patch sequences**:
   - **Sequence X — "Drop bloat, push feature"** (recommended): squash or revert the 6 bloat commits (`3beccc1`, `8e463fa`, `995bacd`, `aaa6b30`, `101ffe6`, `e69eb87`) and the dirty stashes, then commit the dirty worktree as a final cleanup commit, then push.
   - **Sequence Y — "Keep everything, push as-is"**: commit dirty worktree on top, push the full 26-commit stack to `origin/MindAR-Update`.
3. Output both into `./plan/20260808_PUSH_SEQUENCE_<X|Y>.md`.

### Phase C — Stash & Worktree Hygiene `[USER APPROVAL]`

**Owner:** user (no agent edits)
**Risk if done wrong:** loss of mid-stack WIP from `stash@{0}`
**Actions:**

1. Inspect each stash interactively: `git stash show -p stash@{0}`, `git stash show -p stash@{1}`. *(Auto-review has flagged this; requires explicit user approval to proceed.)*
2. Decide per stash: **keep / drop / create-branch-and-apply**. Suggest:
   - `stash@{0}` → create `recovered/mindar-update-stash0` branch off `a5743a4`, then `git stash pop` onto it. Never pop onto `MindAR-Update` HEAD.
   - `stash@{1}` → `git stash drop stash@{1}` after confirming contents are obsolete Unity work. (Safer: also branch first, drop after.)
   - `stash@{2}` → ignore (different branch's stash).
3. **Approval gate:** user reviews each stash and explicitly types "approved" before any drop/pop/branch action.

### Phase D — Worktree Isolation `[USER APPROVAL]`

**Owner:** user
**Why:** the 25-commit stack is a single linear chain on `MindAR-Update`. There is no second worktree of this branch. The other worktree (`codex/sdlc-profile-navigation-fixes`) is on a different branch and is independent.
**Actions:**

1. Confirm `git worktree list` matches expectation (✅ already verified).
2. No new worktree required for Sequence X; if Sequence Y is chosen and the push must be re-tried, consider creating a fresh worktree off `origin/MindAR-Update` and replaying the 25 commits there as a safe scratch space.

### Phase E — Diff Inspection `[USER APPROVAL]`

**Owner:** orchestrator (read-only with explicit consent)
**Actions:**

1. Read each of the 9 dirty files in full (`git diff HEAD` and `git show HEAD~24..HEAD:<path>`) to produce an accurate redacted summary the user can review.
2. **No `git add` / `git commit` / `git restore` / `git stash pop` / `git checkout` operations.** Read-only.

### Phase F — Patch Drafting `[USER APPROVAL]`

**Owner:** orchestrator + planner subagents (in parallel) + reviewer (audit)
**Outputs:** `./report/REVIEW_20260808_REMEDIATION.md` and `./report/PUSH_PLAN_20260808.md`
**Actions:**

1. Choose Sequence X or Y (per Phase B user decision).
2. Draft the exact `git` command list with `--dry-run` flags where possible. Examples (NOT executed):
   - Sequence X preview: `git rebase -i --no-commit af5df75` with `drop` for the 6 bloat commits.
   - Sequence Y preview: `git add -A && git commit --dry-run` to verify the dirty worktree commits cleanly.
3. **Mandatory:** every destructive command (`rebase`, `reset`, `drop`, `clean -fd`, `restore --staged --worktree`, `push --force-with-lease`) must be shown to the user before execution.

### Phase G — Backup Snapshot `[USER APPROVAL — REQUIRED BEFORE PHASE H]`

**Owner:** orchestrator
**Why:** this is the last recovery point before any history rewrite.
**Actions:**

1. Create a backup branch without rewriting history: `git branch backup/mindar-update-pre-remediation 1e5b58f`.
2. Create a backup tag for the dirty worktree: `git stash push -u -m "pre-remediation-dirty-snapshot" -- $(git ls-files --others --exclude-standard --modified --deleted)` (or, with user approval, `git stash --include-untracked -- $(...)`).
3. Record both names in the patch plan so the user can roll back with a single command.

### Phase H — Execution `[USER APPROVAL — ONE FINAL CHECK]`

**Owner:** orchestrator or user (user preference)
**Action:** run the verified command list. After each command, the orchestrator pauses for the user to confirm the next step.

### Phase I — Verification `[AUTO]`

**Owner:** tester subagent
**Actions:**

1. `npm ci` in `frontend-web/`.
2. `npm run lint`.
3. `npm run test`.
4. `npm run build`.
5. `npm run ar:catalog:verify` (offline) and `npm run ar:catalog:verify-remote` (online; user must authorize network).
6. Backend: `pytest tests/ -v --cov=.` in `backend/`.
7. `git status --porcelain` should be empty.
8. `git log --oneline HEAD..origin/MindAR-Update` should be empty (no divergence).
9. `git log --oneline origin/MindAR-Update..HEAD -- [paths-to-push]` should match the expected push list.

### Phase J — Push & Post-Push Validation `[USER APPROVAL]`

**Owner:** git-manager subagent
**Actions:**

1. `git push origin MindAR-Update` (no force). If rejected, abort and surface to user.
2. Confirm CI run is green (`gh run watch` or webhook). Surface failures immediately.
3. **Do NOT** open a PR or merge to `main` without explicit user authorization.

---

## 5. Clarifying Decisions Needed Before Implementation

These are the questions whose answers determine which Sequence (X vs Y) we run. **Nothing in §4 phases C–J can start until these are answered.**

1. **Push target & divergence tolerance.**
   - Is `origin/MindAR-Update` still single-author (you), or has anyone else pushed since `af5df75`?
   - Is `git push` (non-force) acceptable, or do you require `--force-with-lease` semantics for safety?

2. **Bloat tolerance.**
   - Keep the 6 bloat commits (3beccc1 / 8e463fa / 995bacd / aaa6b30 / 101ffe6 / e69eb87) for historical completeness?
   - Or drop them so the push contains only the AR-contract hardening + Unity/Cursor/MCP additions + dirty-worktree migration cleanup?

3. **Dirty-worktree intent.**
   - Is the dirty worktree (deleting `mergeMindTargets.ts` / `mindBuffer` plumbing) **the intended final state**, ready to commit as the new HEAD?
   - Or do you want to keep the legacy runtime-merge path available behind a feature flag (`VITE_PERSISTENT_MIND_VIEWER`)?

4. **Stash `@{0}` fate.**
   - Inspect first (requires Auto-review approval), then: drop / branch-and-keep / branch-and-apply-elsewhere?

5. **Backend validator apply mode.**
   - The drift-gate passes in CI (`--action warn`). Do you ever intend to run `--apply` against production? If yes, the `apply_ar_objects_validator.py` CLI needs a DB-admin handoff (it's gated behind `MONGO_URL + --expected-db`).

6. **Unity test gate.**
   - Today CI has **no Unity job**. The 5 Unity-touching unpushed commits are not CI-gated. Do you want a Unity test job added to `ci.yml` as part of this push (a `.meta` file change + `ComboEditorPlayTest` needs an Editor test runner), or accept that Unity is unverified?

7. **`test-results/` artifact policy.**
   - Should `frontend-web/test-results/` be added to `.gitignore` going forward (it's already there for some patterns but apparently not all)? If yes, this push should include that `.gitignore` change and a follow-up cleanup commit that removes the committed `test-results/` tree from history.

8. **`feature/flashcard-dual-export-course-editor` stash (`stash@{2}`).**
   - Out of scope for this branch. Confirm it's safe to ignore? (It's on a different branch.)

9. **Merge to `main`?**
   - Should the orchestrator coordinate a PR/merge of `MindAR-Update` → `main` after push, or stop at "pushed to `MindAR-Update` only"?

10. **Workspace isolation.**
    - The other worktree (`Edu-platform-sdlc-implementation`) is on `codex/sdlc-profile-navigation-fixes`. Should it be left alone, or should we coordinate cross-worktree changes (the dirty worktree has nothing to do with it, but worth confirming)?

---

## 6. Appendix — Commands the Orchestrator Will NOT Run Without Explicit Approval

To make the safety boundary explicit, the following operations require an unambiguous `[USER APPROVAL]` token in the next message:

- `git add` (any path or `-A`)
- `git commit` (any form, including `--amend`)
- `git push` (any form, including `--force-with-lease`)
- `git fetch` + `git merge` from `origin/MindAR-Update`
- `git rebase` (any form, including `--interactive`)
- `git reset` (any form, including `--hard` / `--mixed` / `--soft`)
- `git stash` `pop` / `drop` / `apply`
- `git checkout` to a different branch or commit
- `git branch -d` / `git branch -D` / `git branch -m`
- `git tag` creation
- `git remote set-url` / `git remote add`
- `git clean` (any flag)
- `git restore` with `--staged` or `--worktree`
- `npm` or `pnpm` `install` / `ci` that mutates lockfiles
- Any `python -m database.migrations.apply_ar_objects_validator --apply`
- `gh pr create` / `gh pr merge` / `gh repo edit`
- `git worktree add` / `git worktree remove`

Until the user says otherwise, this list is the operative safety contract.

---

## 7. Source-of-Truth File References

- Plan: `plan/20260808_MINDAR_UPDATE_REMEDIATION.md` (this file)
- Existing pre-push plan: `plan/IMPLEMENTATION_PLAN_20260722_FREEZE_POSE_SEMANTIC_MANAGER_REVISED.md`
- Existing multi-flashcard plan: `plan/20260706_MULTI_FLASHCARD_FIX_PLAN.md`
- Existing architecture doc: `docs/LEARN_AR_V2_ARCHITECTURE.md`
- Persistent viewer runbook: `docs/report/AR_PERSISTENT_VIEWER_TEST_RUNBOOK.md`
- Debug report (Cause 1/2/3/4/5/6): `docs/report/DEBUG_20260706_MULTI_FLASHCARD_LOADING.md`
- Wiring audit: `docs/report/WIRING_AUDIT.md`
- Backend test files: `backend/tests/test_ar_object_consistency.py`, `test_ar_objects_validator.py`, `test_ar_objects_validator_blocked_in_ci.py`, `test_repair_ar_objects_consistency.py`, `test_audit_ar_objects_consistency.py`, `test_admin_ar_object_consistency.py`
- Validator: `backend/database/migrations/apply_ar_objects_validator.py`
- Repair: `backend/database/migrations/repair_ar_objects_consistency.py`
- Audit: `backend/database/migrations/audit_ar_objects_consistency.py`
- CI: `.github/workflows/ci.yml`
- Frontend scripts: `frontend-web/package.json`, `frontend-web/scripts/verifyRemoteMindCatalog.mjs`, `frontend-web/scripts/vendor-jsqr.cjs`
- Frontend test files: `frontend-web/src/__tests__/mindCatalogContract.test.ts`, `frontend-web/src/__tests__/arCatalogContract.test.ts`, `frontend-web/src/__tests__/LearnARV2.catalogFlow.test.tsx`, `frontend-web/src/__tests__/arViewerBootstrapContract.test.ts`, `frontend-web/src/__tests__/frontendDependencyBoundary.test.ts`
- E2E: `frontend-web/tests/e2e/persistent-mind-viewer.spec.ts`
- Stash & worktree state: `git stash list`, `git worktree list` (verified in §1.1)

---

**End of plan.** No edits, no stages, no commits, no pushes performed. Awaiting user decisions on §5 to proceed.