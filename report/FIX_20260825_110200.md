# Fix Report — Extract Daily Challenge into Standalone Page

**Task:** Tách section `Daily Challenge` từ Profile page thành một page riêng tại route `/daily-challenge`.

---

## Input Summary
**Source:** Orchestrator task
**Total Changes:** 3 files modified, 1 file created

---

## Files Created

### `frontend/src/pages/DailyChallengePage.tsx` ✅
Standalone page trả ra:
- **Loading skeleton** — 3 placeholder blocks (animate-pulse)
- **Error state** — với nút "Try again"
- **Empty state** — khi `daily_challenge` không có data
- **Challenge card** — title + icon + complete badge
- **Progress section** — progress bar (red → green khi complete) + `{progress}/{target}` + %
- **Reward section** — reward text + contextual CTA message
- **Action buttons** — "Go to Courses" + "View Progress"

Data source: `apiClient.getMyProfile().daily_challenge` (cùng contract như Profile.tsx).
Style: clay-bg-playful background, coral/red gradient header, responsive padding (pb-24 mobile / pb-8 md).

---

## Files Modified

### `frontend/src/App.tsx` ✅
- Added import: `import { DailyChallengePage } from "./pages/DailyChallengePage";`
- Added route: `<Route path="/daily-challenge" element={<RequireUserAuth><Layout><DailyChallengePage /></Layout></RequireUserAuth>} />`
- Route placed after `/progress` (grouped with other gamification pages)

### `frontend/src/components/Sidebar.tsx` ✅
- Added `challenge` to `NavItem.iconKey` union type
- Added `ChallengeIcon` SVG component (target/bullseye icon)
- Added `challenge: ChallengeIcon` to `iconComponents` map
- Added nav item: `{ path: '/daily-challenge', label: 'Daily Challenge', shortLabel: 'Challenge', iconKey: 'challenge' }` in `fullNavItems`

---

## Verification Results
- [x] TypeScript `tsc --noEmit` — passed (0 errors)
- [x] `npx vite build` — passed (1445 modules, built in 40s)
- [x] ReadLints on all modified files — no linter errors
- [x] Profile page unchanged (Daily Challenge card remains in place as entry point)
- [x] No data loss — same `apiClient.getMyProfile()` contract used
- [x] No logic broken — all existing functionality preserved

---

## Route Summary
| Route | Component | Auth |
|-------|-----------|------|
| `/daily-challenge` | `DailyChallengePage` | `RequireUserAuth` + `Layout` |

---

## Next Steps
- Navigate to `http://localhost:3000/daily-challenge` to verify runtime behavior
- Add a link from the card in `Profile.tsx` to `/daily-challenge` (optional — the card stays as-is, but a "View details" link could be added)
