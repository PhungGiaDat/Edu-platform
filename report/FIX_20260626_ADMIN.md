# Fix Report - Admin Dashboard Issues

**Date:** 2026-06-26  
**Mode:** YOLO  
**Source:** Code Review, Security Review, Bugbot  
**Total Issues Fixed:** 17

## Input Summary
- **CRITICAL Issues:** 2
- **HIGH Issues:** 3  
- **IMPORTANT Issues:** 7
- **SUGGESTIONS:** 5

---

## Fixes Implemented

### 1. [CRITICAL] Missing RBAC on Admin Endpoints - Teacher Role Check ✅

**File:** `backend/core/security.py`  
**Lines:** 141-175

**Fix Applied:**
```python
async def get_current_teacher(
    current_user: UserDocument = Depends(get_current_user),
) -> UserDocument:
    """
    FastAPI dependency to ensure the current user has teacher role
    
    Checks:
    - is_superuser flag (admin/teacher elevated privileges)
    - role field equals 'teacher' or 'admin'
    
    Returns 403 Forbidden if user is not a teacher/admin.
    """
    # Check is_superuser first (backward compatible)
    if current_user.is_superuser:
        return current_user
    
    # Check role field if it exists
    role = getattr(current_user, 'role', None)
    if role in ('teacher', 'admin'):
        return current_user
    
    # Check roles array if it exists
    roles = getattr(current_user, 'roles', [])
    if isinstance(roles, list) and ('teacher' in roles or 'admin' in roles):
        return current_user
    
    # User is authenticated but not a teacher - deny access
    raise HTTPException(
        status_code=status.HTTP_403_FORBIDDEN,
        detail="Teacher privileges required to access admin dashboard"
    )
```

**Status:** ✅ Fixed

---

### 2. [CRITICAL] Admin API Uses Wrong Dependency ✅

**File:** `backend/api/admin.py`  
**Lines:** 36-40

**Fix Applied:**
```python
def get_admin_repo(
    current_user: UserDocument = Depends(get_current_teacher)  # Changed from get_current_user
) -> AdminRepository:
    """Dependency to get admin repository scoped to current user"""
    return AdminRepository(teacher_id=str(current_user.id))
```

**Status:** ✅ Fixed

---

### 3. [CRITICAL] Frontend Role Check for Admin Routes ✅

**File:** `frontend-web/src/App.tsx`  
**Lines:** 193-208, 259-266

**Fix Applied:**
```tsx
const RequireTeacherRole: React.FC<{ children: React.ReactNode }> = ({ children }) => {
  const { user, isLoading } = useAuth();
  if (isLoading) return null;
  
  // Check if user has teacher/admin privileges
  const hasTeacherRole = user?.role === 'teacher' || user?.role === 'admin' || user?.is_superuser;
  
  if (!hasTeacherRole) {
    // Redirect non-teachers away from admin pages
    return <Navigate to="/profile" replace />;
  }
  
  return <>{children}</>;
};

// Admin Routes wrapped with RequireTeacherRole
<Route path="/admin" element={<RequireTeacherRole><AdminErrorBoundary><AdminDashboard /></AdminErrorBoundary></RequireTeacherRole>} />
```

**Status:** ✅ Fixed

---

### 4. [HIGH] N+1 Query in Student Progress ✅

**File:** `backend/repositories/admin_repository.py`  
**Lines:** 374-396

**Fix Applied:**
```python
async def get_student_progress(self, user_id: str) -> Dict[str, Any]:
    """Get detailed progress for a student"""
    student = await self.get_student_by_id(user_id)
    if not student:
        return {}
    
    # Get course details - batch fetch all courses in ONE query (fixes N+1)
    course_ids = [e["course_id"] for e in student.get("enrollments", [])]
    courses = []
    if course_ids:
        # Batch fetch all courses using $in operator
        cursor = self.courses_collection.find({
            "course_id": {"$in": course_ids},
            "teacher_id": self.teacher_id
        })
        courses = await cursor.to_list(length=len(course_ids))
    
    # Enrich enrollments with course info using O(1) lookup
    course_map = {c["course_id"]: c for c in courses}
    for enrollment in student.get("enrollments", []):
        course = course_map.get(enrollment["course_id"], {})
        enrollment["course_title"] = course.get("title", "Unknown")
        enrollment["course_thumbnail"] = course.get("thumbnail_url", "")
    
    return student
```

**Status:** ✅ Fixed

---

### 5. [HIGH] Regex ReDoS Vulnerability ✅

**File:** `backend/repositories/admin_repository.py`  
**Lines:** 347-351

**Fix Applied:**
```python
import re  # Added import

async def get_students(self, skip: int = 0, limit: int = 20, search: Optional[str] = None):
    query: Dict[str, Any] = {"teacher_id": self.teacher_id}
    
    if search:
        # Fix ReDoS vulnerability: escape special regex chars and limit length
        escaped_search = re.escape(search.strip())[:50]
        query["$or"] = [
            {"user_name": {"$regex": escaped_search, "$options": "i"}},
            {"user_id": {"$regex": escaped_search, "$options": "i"}}
        ]
```

**Status:** ✅ Fixed

---

### 6. [HIGH] recent_activity Wrong Slice ✅

**File:** `backend/api/reports.py`  
**Line:** 129

**Fix Applied:**
```python
# Changed from [-days:] to [:days] to get MOST RECENT days instead of oldest
"recent_activity": recent_activity[:days] if recent_activity else [],
```

**Status:** ✅ Fixed

---

### 7. [IMPORTANT] Dashboard Stats Parallel DB Calls ✅

**File:** `backend/repositories/admin_repository.py`  
**Lines:** 42-76

**Fix Applied:**
```python
import asyncio  # Added import

async def get_dashboard_stats(self) -> Dict[str, Any]:
    """Get dashboard statistics for the teacher - optimized with parallel queries"""
    # ... setup code ...
    
    # Parallelize the 4 count_documents calls using asyncio.gather
    async def count_students():
        return await self.student_progress_collection.count_documents({"teacher_id": self.teacher_id})
    
    async def count_courses():
        return await self.courses_collection.count_documents({"teacher_id": self.teacher_id})
    
    async def count_flashcards():
        return await self.flashcards_collection.count_documents({
            "teacher_id": self.teacher_id, "is_active": True
        })
    
    async def count_decks():
        return await self.flashcard_decks_collection.count_documents({
            "teacher_id": self.teacher_id, "is_active": True
        })
    
    # Execute all counts in parallel
    student_count, course_count, flashcard_count, deck_count = await asyncio.gather(
        count_students(),
        count_courses(),
        count_flashcards(),
        count_decks()
    )
```

**Status:** ✅ Fixed

---

### 8. [IMPORTANT] Hard Delete vs Soft Delete ✅

**File:** `backend/repositories/admin_repository.py`  
**Lines:** 193-199

**Fix Applied:**
```python
async def delete_course(self, course_id: str) -> bool:
    """Soft delete a course - sets is_active=False and deleted_at instead of removing"""
    result = await self.courses_collection.update_one(
        {"course_id": course_id, "teacher_id": self.teacher_id},
        {"$set": {"is_active": False, "deleted_at": datetime.utcnow()}}
    )
    return result.modified_count > 0
```

**Status:** ✅ Fixed

---

### 9. [IMPORTANT] useEffect Missing Dependencies ✅

**File:** `frontend-web/src/pages/admin/Dashboard.tsx`  
**Lines:** 1-24

**Fix Applied:**
```tsx
import React, { useEffect, useState, useCallback } from 'react';
// ...
const Dashboard: React.FC = () => {
  // ...
  const loadStats = useCallback(async () => {
    try {
      const data = await adminDashboardApi.getStats();
      setStats(data);
    } catch (error) {
      console.error('Failed to load dashboard stats:', error);
    } finally {
      setLoading(false);
    }
  }, []);

  useEffect(() => {
    loadStats();
  }, [loadStats]);  // Added loadStats to dependency array
```

**Status:** ✅ Fixed

---

### 10. [IMPORTANT] Replace alert() with Toast ✅

**File:** `frontend-web/src/pages/admin/GoalSettings.tsx`  
**Lines:** 1-35, 64-79, 91-97

**Fix Applied:**
```tsx
const GoalSettings: React.FC = () => {
  const [toast, setToast] = useState<{ message: string; type: 'success' | 'error' } | null>(null);
  // ...
  
  const handleSubmit = async (e: React.FormEvent) => {
    // ...
    setToast({ message: t('admin.goalSettings.saved'), type: 'success' });
    // ...
    setToast({ message: t('admin.goalSettings.saveFailed'), type: 'error' });
  };

  // Auto-dismiss toast after 3 seconds
  useEffect(() => {
    if (toast) {
      const timer = setTimeout(() => setToast(null), 3000);
      return () => clearTimeout(timer);
    }
  }, [toast]);
  
  return (
    <AdminLayout>
      {/* Toast Notification */}
      {toast && (
        <div className={`
          fixed top-4 right-4 z-50 px-6 py-3 rounded-xl shadow-lg
          ${toast.type === 'success' ? 'bg-green-500' : 'bg-red-500'}
          text-white font-medium animate-fade-in
        `}>
          {toast.message}
        </div>
      )}
      {/* ... */}
```

**Status:** ✅ Fixed

---

### 11. [IMPORTANT] Mobile Chart Overflow ✅

**File:** `frontend-web/src/pages/admin/Analytics.tsx`  
**Lines:** 116-130, 161-186

**Fix Applied:**
```tsx
{/* Activity by Day */}
<SectionCard title={t('admin.analytics.activityByDay')}>
  <div className="h-48 sm:h-48 overflow-x-auto">
    <div className="min-w-[300px] h-full flex items-end justify-around gap-2">
      {/* ... chart content ... */}
    </div>
  </div>
</SectionCard>

{/* Progress Trends */}
<SectionCard title={t('admin.analytics.progressTrends')} className="mt-6">
  <div className="h-32 sm:h-48 overflow-x-auto">
    <div className="min-w-[400px] h-full flex items-end justify-between gap-1">
      {/* ... chart content ... */}
    </div>
  </div>
</SectionCard>
```

**Status:** ✅ Fixed

---

### 12. [IMPORTANT] Add ARIA Labels ✅

**File:** `frontend-web/src/pages/admin/StudentList.tsx`  
**Lines:** 86-106

**Fix Applied:**
```tsx
<input
  type="text"
  aria-label={t('admin.students.searchPlaceholder')}
/>
<button
  type="submit"
  aria-label={t('admin.common.search')}
>
```

**Status:** ✅ Fixed

---

### 13. [IMPORTANT] Full Pagination Implementation ✅

**File:** `frontend-web/src/pages/admin/StudentList.tsx`  
**Lines:** 1-250 (full refactor)

**Fix Applied:**
- Added `memo` for `StudentRow` component to memoize list items
- Added full pagination controls: first/prev/next/last buttons
- Added "Showing X-Y of Z" indicator
- Added `goToPage` function for navigation

**Status:** ✅ Fixed

---

### 14. [IMPORTANT] Create AdminErrorBoundary ✅

**File:** `frontend-web/src/components/admin/AdminErrorBoundary.tsx` (NEW)

**Implementation:**
```tsx
class AdminErrorBoundary extends Component<Props, State> {
  static getDerivedStateFromError(error: Error): Partial<State> {
    return { hasError: true, error };
  }

  componentDidCatch(error: Error, errorInfo: ErrorInfo): void {
    console.error('[AdminErrorBoundary] Caught error:', error, errorInfo);
  }

  render(): ReactNode {
    if (this.state.hasError) {
      return <AdminErrorFallback error={this.state.error} resetError={this.resetError} />;
    }
    return this.props.children;
  }
}
```

Wrapped all admin routes with `<AdminErrorBoundary>`

**Status:** ✅ Fixed

---

### 15. [IMPORTANT] Add MongoDB Compound Indexes ✅

**File:** `backend/models/admin_models.py`  
**Lines:** Multiple

**Fix Applied:**
```python
class Settings:
    # flashcard_decks
    indexes = [
        ("teacher_id", "is_active"),  # NEW compound index
    ]
    
    # courses
    indexes = [
        ("teacher_id", "is_active"),  # NEW compound index
        ("teacher_id", "created_at"),  # NEW compound index
        ("teacher_id", "is_published"),  # NEW compound index
    ]
    
    # student_progress
    indexes = [
        ("teacher_id", "last_active"),  # NEW compound index
        ("teacher_id", "total_xp"),  # NEW compound index
    ]
```

**Status:** ✅ Fixed

---

### 16. [SUGGESTION] Hard-coded User Profile ✅

**File:** `frontend-web/src/components/admin/AdminLayout.tsx`  
**Lines:** 1-80

**Fix Applied:**
```tsx
import { useAuth } from '../../contexts/AuthContext';

const AdminSidebar: React.FC = () => {
  const { user } = useAuth();
  
  const getInitials = () => {
    if (user?.name) {
      return user.name.split(' ').map(n => n[0]).join('').toUpperCase().slice(0, 2);
    }
    if (user?.email) {
      return user.email[0].toUpperCase();
    }
    return 'T';
  };
  
  return (
    {/* User Profile - Now using actual auth user data */}
    <div className="flex items-center gap-3 px-4 py-3">
      <div className="w-10 h-10 rounded-full bg-gradient-to-br from-[#6EB9FF] to-[#B4E197] flex items-center justify-center text-white font-medium">
        {getInitials()}
      </div>
      <div className="flex-1 min-w-0">
        <p className="text-white text-sm font-medium truncate">{user?.name || 'Teacher'}</p>
        <p className="text-white/50 text-xs truncate">{user?.email || ''}</p>
      </div>
    </div>
  );
};
```

**Status:** ✅ Fixed

---

### 17. [SUGGESTION] Missing Vietnamese Translations ✅

**Files:** `vi/admin.json`, `en/admin.json`

**Added Translations:**
- `students.showing` - "Showing X-Y of Z" / "Hiển thị X-Y trong Z"
- `students.firstPage`, `students.previousPage`, `students.nextPage`, `students.lastPage`
- `errorBoundary.title`, `errorBoundary.message`, `errorBoundary.tryAgain`, `errorBoundary.goHome`

**Status:** ✅ Fixed

---

### 18. [SUGGESTION] List Items Memoization ✅

**File:** `frontend-web/src/pages/admin/StudentList.tsx`

**Fix Applied:**
```tsx
import React, { useEffect, useState, useCallback, memo } from 'react';

// Memoized student row component
const StudentRow = memo<{
  student: StudentProgress;
  onClick: () => void;
  t: (key: string) => string;
  i18n: { language: string };
}>(({ student, onClick, t, i18n }) => (
  // ... row content ...
));
```

**Status:** ✅ Fixed

---

### 19. [SUGGESTION] Consistent Error Handling in adminApi.ts ✅

**File:** `frontend-web/src/services/adminApi.ts`

**Fix Applied:**
Added try/catch wrappers with console.error logging to all API methods:
```tsx
async getStats(): Promise<DashboardStats> {
  try {
    const response = await apiClient.get<DashboardStats>(`${ADMIN_BASE_URL}/dashboard`);
    return response.data;
  } catch (error) {
    console.error('[adminDashboardApi.getStats] Error:', error);
    throw error;
  }
},
// ... same pattern for all other methods
```

**Status:** ✅ Fixed

---

## Summary Table

| ID | Title | Severity | Status |
|----|-------|----------|--------|
| 1 | Missing RBAC - get_current_teacher() | CRITICAL | ✅ Fixed |
| 2 | Admin API Uses Wrong Dependency | CRITICAL | ✅ Fixed |
| 3 | Frontend Role Check | CRITICAL | ✅ Fixed |
| 4 | N+1 Query in Student Progress | HIGH | ✅ Fixed |
| 5 | Regex ReDoS Vulnerability | HIGH | ✅ Fixed |
| 6 | recent_activity Wrong Slice | HIGH | ✅ Fixed |
| 7 | Dashboard Stats Sequential DB Calls | IMPORTANT | ✅ Fixed |
| 8 | Hard Delete vs Soft Delete | IMPORTANT | ✅ Fixed |
| 9 | useEffect Missing Dependencies | IMPORTANT | ✅ Fixed |
| 10 | Form Uses alert() | IMPORTANT | ✅ Fixed |
| 11 | Mobile Chart Overflow | IMPORTANT | ✅ Fixed |
| 12 | Missing ARIA Labels | IMPORTANT | ✅ Fixed |
| 13 | Pagination Missing | IMPORTANT | ✅ Fixed |
| 14 | No Error Boundaries | IMPORTANT | ✅ Fixed |
| 15 | Missing MongoDB Indexes | IMPORTANT | ✅ Fixed |
| 16 | Hard-coded User Profile | SUGGESTION | ✅ Fixed |
| 17 | Missing Vietnamese Translations | SUGGESTION | ✅ Fixed |
| 18 | List Items Not Memoized | SUGGESTION | ✅ Fixed |
| 19 | Inconsistent Error Handling | SUGGESTION | ✅ Fixed |

---

## Verification Results

- [x] All 17 issues implemented
- [x] Backend security RBAC properly enforced
- [x] Frontend role checking in place
- [x] N+1 query fixed with batch fetch
- [x] ReDoS vulnerability patched
- [x] Performance optimizations applied
- [x] UI/UX improvements completed
- [x] Accessibility improvements (ARIA labels)
- [x] Error handling improvements
- [x] Translations completed

---

## Files Modified

**Backend:**
- `backend/core/security.py` - RBAC enforcement
- `backend/api/admin.py` - Updated dependency injection
- `backend/repositories/admin_repository.py` - N+1 fix, parallel queries, soft delete, ReDoS fix
- `backend/api/reports.py` - Slice fix
- `backend/models/admin_models.py` - Compound indexes

**Frontend:**
- `frontend-web/src/App.tsx` - Role check wrapper, error boundaries
- `frontend-web/src/pages/admin/Dashboard.tsx` - useCallback fix
- `frontend-web/src/pages/admin/GoalSettings.tsx` - Toast notifications
- `frontend-web/src/pages/admin/Analytics.tsx` - Responsive chart containers
- `frontend-web/src/pages/admin/StudentList.tsx` - ARIA, pagination, memoization
- `frontend-web/src/components/admin/AdminLayout.tsx` - Real user data
- `frontend-web/src/components/admin/AdminErrorBoundary.tsx` - NEW file
- `frontend-web/src/services/adminApi.ts` - Consistent error handling
- `frontend-web/src/i18n/locales/en/admin.json` - New translations
- `frontend-web/src/i18n/locales/vi/admin.json` - New translations

**Next Step:** Hand off to tester for regression testing
