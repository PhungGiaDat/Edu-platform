# ============================================
# AR-FLASHCARD BACKEND DEPENDENCIES
# ============================================

# === Core Framework ===
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
sentry-sdk>=2.0.0
pydantic>=2.9.0
pydantic-settings>=2.5.0
email-validator>=2.0.0
pywebpush>=1.14.0

# === Database - MongoDB (Beanie ODM) ===
motor>=3.6.0
pymongo>=4.10.0
dnspython>=2.6.0
beanie>=1.27.0,<2.0.0

# === Database - PostgreSQL ORM / migrations ===
asyncpg>=0.31.0
SQLAlchemy>=2.0.44,<2.1.0
alembic>=1.14.0,<2.0.0

# === Redis (Caching, Sessions, Rate Limiting) ===
redis>=5.0.0

# === Authentication ===
PyJWT>=2.9.0
python-jose[cryptography]>=3.3.0
passlib[bcrypt,argon2]>=1.7.4
bcrypt>=4.1.0  # Explicit bcrypt requirement for password hashing backend
argon2-cffi>=23.1.0  # Use argon2 for better security and reliability

# === Environment & Utils ===
python-dotenv>=1.0.0
python-multipart>=0.0.12

# === WebSocket ===
websockets>=13.0

# === HTTP Client ===
httpx>=0.27.0

# === AI - LangChain (Core only - no numpy conflict) ===
langchain-core>=0.3.0
langchain-google-genai>=2.0.0
langchain-openai>=0.1.0
google-generativeai>=0.8.0

# === LLM Retry & Resilience ===
tenacity>=8.2.0

# === Vector Search - Qdrant Cloud Inference (no local embedding model) ===
qdrant-client>=1.14.2,<2.0.0

# === Speech-to-Text (Server-side fallback for Safari/Firefox) ===
# faster-whisper uses CTranslate2 for efficient CPU inference
# Only ~150MB RAM for 'tiny' model
faster-whisper>=1.0.0

# === Production Server ===
gunicorn>=23.0.0
