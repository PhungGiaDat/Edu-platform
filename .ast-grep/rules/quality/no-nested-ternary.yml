id: no-nested-ternary
language: TypeScript
severity: suggestion
message: "Avoid nested ternary expressions — they hurt readability"
note: "Use if/else, switch, or a lookup map instead"
rule:
  pattern: $A ? $B ? $$$NESTED : $$$ : $$$