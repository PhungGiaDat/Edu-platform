id: no-empty-catch
language: TypeScript
severity: warning
message: "Empty catch block silently swallows errors"
note: "Log the error, rethrow, or use a default fallback"
rule:
  pattern: |
    try {
      $$$BODY
    } catch ($E) {
    }