# Context Snapshot — 20260901_010500

## Task
Original: build "Tra từ & Sổ tay" (Dictionary + Notebook) feature for the mobile-web app (`frontend/`) with wiki-backed lookup, claymorphism vibrant style, children-safety (ages 5–8). Expanded during session to: LLM provider health + B.AI fallback, Qdrant cross-lingual retrieval fix, RAG bot de-Mongo, and full backend de-Mongo (Postgres = single source of truth).

## Mode
INTERACTIVE (Phase gates 1–2, wave approvals) → YOLO (review/fix loops, W2–W6) · Deployment phase SKIPPED by product owner directive · Docs phase lightweight (progress + ticks only).

## Phase Progress
| Phase | Status | Key Outcome |
|-------|--------|-------------|
| Phase 1: Planning | ✅ Done | spec + 14-task TDD plan + live-verified wiki API research; ages 5–8 directive integrated |
| Phase 2: Design UI/UX | ✅ Done | wireframes 390px, states matrix, taste-skill product-UI adaptation (dials 3/3/3) |
| Phase 3: Development | ✅ Done | Tasks 1–12 commit-per-task + 3 taste pre-flight fixes |
| Phase 4: Code Review | ✅ Done | 10 issues (1 Critical blocklist bypass) → 9 fixed, ISSUE-007 deferred then closed |
| Phase 5: Testing | ✅ Done | backend 728 passed (5 pre-existing fails) · frontend 16 focused · live Supabase journey verified |
| Phase 6: Deployment | ⏭️ Skipped | product owner directive — deploy is user's action |
| Phase 7: Documentation | ✅ Done | progress evidence + plan ticks (81/81) + env docs |
| Extensions: LLM health/B.AI · Qdrant VI expansion · RAG de-Mongo · De-Mongo W1–W6 · API audit fixes | ✅ Done | 15/15 repos Postgres-only · 39/39 audited endpoints PASS |

## Key Decisions
- Wiki chain: Simple English Wikipedia → English Wikipedia → Wiktionary definitions (ages 5–8); only `type == "standard"` cached; CC BY-SA attribution.
- Safety chain: plain-term blocklist + whole-word space-bypass matching, `<<<USER_CONTENT>>>` prompt fencing, word-charset validation, deterministic reranker (0.6 vector + 0.4 lexical).
- LLM providers: TokenRouter primary → B.AI fallback with **sticky preferred provider** + generation-aware startup ping (1-token completion, not /models) + permanent-error fast failover (model_not_found = 0 retries).
- Qdrant cross-lingual: VI→EN animal lexicon expansion (45 pairs) + dual-query (expanded + EN-only) — all-MiniLM-L6-v2 is English-only.
- De-Mongo: adapter strategy (not 264 hand-written methods), 6 waves, `postgres_core_enabled` gates removed, Mongo boot non-fatal, `MONGO_URL` optional, `database_engine = "postgres"`.
- Agent fleet: 31 `.opencode/agents/*.md` + opencode.json on B.AI models (glm-5.3-flash/deepseek-v4-flash/qwen3.8-flash/mimo-v2.5 by role).
- Docs convention: `docs/frontend-web/` (spec/plan/ui-design/progress) — one source of truth.

## Files Changed
| File | Action | Notes |
|------|--------|-------|
| `backend/services/{content_safety_service,prompt_guard,wikipedia_service,retrieval_reranker,llm_health}.py` | Created | safety, injection guard, wiki+wiktionary, reranker, provider health |
| `backend/services/dictionary_service.py` | Rewritten | v2 hybrid retrieval + `/lookup` |
| `backend/services/agentic_rag_service.py` | Modified | Postgres-only cache/history/progress |
| `backend/services/llm_clients.py` | Modified | B.AI factory + sticky cascade + no-retry permanent errors |
| `backend/repositories/*.py` (15 repos) | Rewritten | Postgres-only (W1–W6) |
| `backend/api/{dictionary,notebook,vocabulary_topics,ai,course_lessons,flashcards,public,combos}.py` | Modified | new endpoints + Postgres-native response models |
| `backend/main.py`, `backend/settings.py` | Modified | ORM lifespan, LLM ping, Mongo non-fatal, MONGO_URL optional, WIKI_*/BAI_* settings |
| `backend/database/postgres/migrations/` (8 files) | Created | notebook base+rich+FK, chat_logs, lessons schema, vector_embedding, session tracking, profile_content |
| `frontend/src/pages/{DictionaryPage,NotebookPage}.tsx` | Rewritten/Modified | two-mode page, detail dialog, practice nav |
| `frontend/src/features/{dictionary,notebook}/**` | Created | DefinitionCard, SentenceTranslateCard, NotebookEntryDetail, icons |
| `frontend/src/shared/components/ui/Button.tsx` | Modified | variant+caller style merge (ISSUE-007) |
| `frontend/src/app/components/Sidebar.tsx`, `contexts/LocaleContext.tsx` | Modified | nav entries + locale keys |
| `frontend/src/design-tokens/claymorphic.ts`, `styles/claymorphic-utilities.css`, `index.html` | Modified | brandColors + clay utilities + Nunito/DM Sans |
| `frontend/src/services/dictionaryApi.ts`, `types/dictionary.ts` | Created | lookup/translate client |
| `docs/frontend-web/{spec,plan,ui-design,progress}/2026-08-30-dictionary-notebook-wiki.md` | Created | feature docs |
| `docs/frontend-web/{plan,progress}/2026-08-31-de-mongo-postgres.md` | Created | de-Mongo plan + evidence |
| `docs/research/20260830_dictionary_notebook_wiki.md` | Created | live-verified wiki API research |
| `docs/context/COMPACT_*.md` | Created | context snapshots |
| `opencode.json`, `.opencode/agents/*.md` | Modified | B.AI model fleet |

## Current Phase
All phases complete — session at natural stopping point. Git: `origin/10-days-quick-run` synced `0 0`, 25+ commits IN-HEAD, working tree clean (only other session's files uncommitted).

## Active Issues / Blockers
- Deployment not done (user action) — code exists only on branch until deployed.
- Server env vars needed: `BAI_API_KEY`, `DATABASE_URL`, `TOKENROUTER_API_KEY` (+ optional `MODEL_FALLBACKS=""` cleanup).
- Real-device pass (Chrome Android / Safari iOS) pending — graduation acceptance gate (emulation only so far).
- 5 pre-existing backend test failures (Beanie helpers, AR validator CI ref, course schema) — unrelated to this work.
- Other concurrent session owns: Daily Challenge/Leaderboard work (`momo` seed files, adaptive-mission docs) — uncommitted by them; do NOT commit/`reset --hard` over it (one reset incident happened and was recovered via cherry-pick).
- Minor leftovers: `AIChatBuddy` 390px overlap partially addressed (FAB owned by other session), difficulty dots color-only (WCAG 1.4.1, pre-existing), stale probe files (`qdrant_*.txt`, `report-source.md`) untracked.

## Next Steps
1. Product owner: deploy `2243fc1`+ (Render backend + Vercel frontend) with the env vars above; run migrations if server DB differs from `edu_platform` (all 8 already applied to Supabase `edu_platform`).
2. Post-deploy: verify `/ai/llm-health` (both providers healthy), Tra từ lookup (`elephant`/`con mèo`), notebook save 201/200, Lexi RAG answers with Qdrant sources.
3. Schedule real-device mobile pass for graduation acceptance.
4. Other session: commit its Daily Challenge/Leaderboard slice.
5. Optional cleanups: delete stale probe artifacts, dark-mode program, Wiktionary sense-limit tuning.

---
*Compacted at 2026-09-01 01:05 — Resume from this snapshot*
